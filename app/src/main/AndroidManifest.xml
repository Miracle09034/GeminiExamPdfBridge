<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Targeting API 28 intentionally keeps this small local utility compatible
         with direct shared-storage file paths when the user grants storage access. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:theme="@style/AppTheme"
        android:label="Gemini Exam PDF Bridge"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            
            <!-- Legacy Action Filter (Kept for backward compatibility) -->
            <intent-filter>
                <action android:name="com.examexporter.bridge.CONVERT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Deep Link Filter for gemini-pdf://render?html=...&pdf=... -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                
                <data
                    android:scheme="gemini-pdf"
                    android:host="render" />
            </intent-filter>

        </activity>
    </application>
</manifest>
